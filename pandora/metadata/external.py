from __future__ import annotations

# TODO: Add external metadata clients here.
# Planned sources include PDBe summary endpoints, UniProt, SIFTS, taxonomy
# services, and any lab-specific metadata adapters. These functions should
# return the same typed metadata records as `pandora.metadata.mmcif`, with
# provenance.source set to the external database name.
