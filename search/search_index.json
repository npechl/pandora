{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Pandora","text":"<p>Pandora is a Python library for turning raw PDB/PDBe data into typed, policy-driven, ML-ready protein structure datasets.</p> <p>Each component, ingestion, parsing, canonicalisation, metadata, annotation, is a plain function you can call on its own, or chain into a pipeline. Nothing is hidden behind a framework object: you pass a <code>Structure</code> in, you get a <code>Structure</code> (or a typed record) out.</p> <p>Warning</p> <p>Pandora is under active development. Ingestion, parsing, canonicalisation, metadata, and annotations are implemented today; dataset curation, similarity/splitting, and provenance manifests are still on the roadmap.</p>"},{"location":"#install","title":"Install","text":"<pre><code># The following commands are not working. \n\n# pip install pandora[ingestion]      # fetch_mmcif() / fetch_list_mmcif()\n# pip install pandora[annotations]    # freesasa-backed annotations\n# pip install pandora[full]           # everything, including the dev/test extras\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>from pathlib import Path\n\nfrom pandora.ingestion import fetch_mmcif\nfrom pandora.parsing import mmcif_to_structure\nfrom pandora.canonicalisation import canonicalise_structure\nfrom pandora.metadata import collect_metadata\nfrom pandora.schemas.canonicalisation import canonicalisationPolicy\n\nentry_id = \"1cbs\"\nmmcif_dir = Path(\"./mmcif\")\n\n# 1. Ingestion \u2014 fetch the raw mmCIF file, with on-disk caching.\nprovenance = fetch_mmcif(\n    entry_id=entry_id,\n    provider=\"pdbe\",\n    source_uri=None,\n    output_dir=mmcif_dir,\n)\n\n# 2. Parsing \u2014 raw mmCIF -&gt; Pandora's typed Structure.\nstructure, diagnostics, status = mmcif_to_structure(\n    str(mmcif_dir / f\"{entry_id}.cif\")\n)\n\n# 3. Canonicalisation \u2014 apply a policy (chain IDs, altlocs, ligands, ...).\npolicy = canonicalisationPolicy(\n    policy_id=\"quickstart\",\n    policy_name=\"Quickstart\",\n    policy_version=\"1.0.0\",\n)\ncanonical, mappings, canon_provenance = canonicalise_structure(structure, policy)\n\n# 4. Metadata \u2014 source-backed entry/entity/quality/taxonomy records.\nmetadata = collect_metadata(canonical)\n</code></pre>"},{"location":"#policies","title":"Policies","text":"<p>Use Policies for how to configure canonicalisation</p>"},{"location":"#recipes","title":"Recipes","text":"<p>See Recipes for more end-to-end configurations of how to build datasets. </p>"},{"location":"#reference","title":"Reference","text":"<p>The full function and schema reference is under Reference.</p>"},{"location":"contribute/","title":"How to contribute","text":"<p>TODO</p>"},{"location":"examples/","title":"Examples","text":""},{"location":"examples/#examplesoverviewpy","title":"<code>examples/overview.py</code>","text":"<p>Fetches PDB entry <code>1cbs</code> from PDBe, parses it, and runs it through three different canonicalisation policies (preserve-as-reported, remap + renumber, and author-centric identifiers). It's the same walkthrough used in Policies.</p> <pre><code>python examples/overview.py\n</code></pre> <p>Requires network access on first run (the fetched mmCIF file is cached under <code>examples/mmcif/</code> afterwards, so subsequent runs are offline).</p>"},{"location":"examples/#dev-dataset","title":"Dev dataset","text":"<p><code>scripts/build_dev_dataset.py</code> builds the small, checked-in mmCIF sample set under <code>datasets/dev/</code> used for local testing without hitting the network. See that script for the entry list and how to regenerate it.</p>"},{"location":"policies/","title":"Policies","text":""},{"location":"policies/#canonicalisation","title":"Canonicalisation","text":"<p><code>canonicalise_structure(structure, policy)</code> never guesses. Every normalization decision, how chains are named, how altlocs are resolved, whether ligands are kept, comes from a <code>canonicalisationPolicy</code> (<code>pandora.schemas.canonicalisation.canonicalisationPolicy</code>). Unset rules fall back to \"preserve as reported\", so an empty policy is a no-op pass that only records provenance.</p> <pre><code>from pandora.schemas.canonicalisation import canonicalisationPolicy\n\npolicy = canonicalisationPolicy(\n    policy_id=\"my-policy\",\n    policy_name=\"My Policy\",\n    policy_version=\"1.0.0\",\n    description=\"\",  # optional, free text\n)\n</code></pre>"},{"location":"policies/#identifier_rules","title":"<code>identifier_rules</code>","text":"<p><code>chain_id.strategy</code> (default <code>preserve</code>)</p> Value Description <code>preserve</code> Keep the original <code>label_asym_id</code> as the canonical chain ID. <code>remap</code> Reassign chain IDs to sequential labels (<code>A</code>, <code>B</code>, ... <code>Z</code>, <code>AA</code>, <code>AB</code>, ...). <code>use_auth_chain_id</code> Use <code>auth_asym_id</code> as the canonical chain ID (falls back to <code>label_asym_id</code> if no author ID is set). <p><code>residue_numbering.strategy</code> (default <code>preserve</code>)</p> Value Description <code>preserve</code> Keep <code>label_seq_id</code> as the canonical <code>seq_id</code>. <code>use_auth_seq</code> Use the numeric part of <code>auth_seq_id</code> as the canonical <code>seq_id</code>. <code>renumber</code> Renumber residues sequentially from 1 per chain, in order of first appearance, ignoring gaps in the original numbering. <p><code>residue_numbering.preserve_insertion_codes</code> (default <code>True</code>) only applies when <code>strategy</code> is <code>preserve</code> or <code>use_auth_seq</code>: <code>True</code> keeps insertion codes, <code>False</code> strips them. It has no effect under <code>renumber</code> \u2014 renumbering always drops insertion codes.</p> <p><code>assembly_id.strategy</code> (default <code>preserve</code>)</p> Value Description <code>preserve</code> Keep original assembly IDs. <code>remap</code> Reassign assembly IDs to sequential integers (<code>1</code>, <code>2</code>, ...) in existing order. <code>standardize</code> Intended to put the preferred biological assembly first, then renumber the rest. <p><code>remap</code> and <code>standardize</code> currently behave identically \u2014 both just renumber sequentially in file order. The \"preferred assembly goes first\" behavior implied by <code>standardize</code> isn't implemented yet.</p>"},{"location":"policies/#missing_data_rules","title":"<code>missing_data_rules</code>","text":"<p><code>missing_atoms.strategy</code> (default <code>annotate</code>) \u2014 a residue is \"missing atoms\" if any backbone <code>N</code>/<code>CA</code>/<code>C</code>/<code>O</code> atom is absent.</p> Value Description <code>preserve</code> Keep residues with missing backbone atoms unchanged, no diagnostic. <code>annotate</code> Keep residues unchanged; emit a <code>MISSING_ATOMS</code> diagnostic per affected residue (if <code>record_missingness</code>). <code>drop_partial_residue</code> Remove residues missing any backbone atom (also diagnosed like <code>annotate</code>). <code>impute</code> Intended to fill missing atom coordinates from ideal geometry, gated by <code>allow_imputation</code>. Not implemented yet \u2014 currently identical to <code>preserve</code>. <p><code>missing_residues.strategy</code> (default <code>annotate</code>) \u2014 a \"gap\" is a jump of more than 1 in a chain's <code>seq_id</code> sequence.</p> Value Description <code>preserve</code> Keep sequence gaps as-is; emits <code>SEQUENCE_GAP</code> diagnostics only if <code>record_gaps</code>. <code>annotate</code> Same effect as <code>preserve</code> \u2014 gaps are flagged, never modified. <code>drop_chain_segment</code> Remove every atom belonging to a chain that contains any gap. <code>impute</code> Intended to model missing residues from sequence + ideal geometry, gated by <code>allow_imputation</code>. Not implemented yet \u2014 no <code>impute</code> branch exists; falls through with the same behavior as <code>preserve</code>/<code>annotate</code>. <p><code>incomplete_chains.strategy</code> (default <code>preserve</code>) \u2014 \"incomplete\" means the chain's residue range has gaps between its first and last residue.</p> Value Description <code>preserve</code> Keep chains regardless of completeness. <code>exclude</code> Drop every asym unit/atom belonging to an incomplete chain. <code>truncate_to_complete_regions</code> Keep only the single longest contiguous run of residues per incomplete chain. <p>Extra flags:</p> Field Description Default <code>missing_atoms.allow_imputation</code> Precondition for <code>strategy: impute</code>. Currently unused since <code>impute</code> isn't implemented. <code>False</code> <code>missing_atoms.record_missingness</code> Emit a <code>MISSING_ATOMS</code> diagnostic for every affected residue. <code>True</code> <code>missing_residues.record_gaps</code> Emit a <code>SEQUENCE_GAP</code> diagnostic for every detected gap. <code>True</code>"},{"location":"policies/#altloc_rules","title":"<code>altloc_rules</code>","text":"<p><code>strategy</code> (default <code>select_best_occupancy</code>)</p> Value Description <code>preserve</code> Keep every altloc variant; no atoms are dropped. <code>select_best_occupancy</code> Per residue, pick the altloc group with the highest mean <code>occupancy</code>; ties broken by <code>tie_breaker</code>. <code>select_first</code> Pick the alphabetically-first altloc label. <code>select_user_defined</code> Pick <code>user_defined_altloc</code> if it's one of the residue's available altlocs, otherwise fall back to the alphabetically-first one. <p><code>tie_breaker</code> (default <code>alphabetical_first</code>) \u2014 only consulted when <code>strategy: select_best_occupancy</code> has a tie.</p> Value Description <code>alphabetical_first</code> Lowest altloc label. <code>alphabetical_last</code> Highest altloc label. <code>lowest_b_factor</code> Lowest mean <code>B_iso_or_equiv</code> across the tied groups. <code>highest_b_factor</code> Highest mean <code>B_iso_or_equiv</code> across the tied groups. <p><code>user_defined_altloc</code> (default <code>None</code>) \u2014 the altloc code to select under <code>strategy: select_user_defined</code>.</p> <p><code>record_selection</code> (default <code>True</code>) \u2014 populate <code>AltlocSelectionMapping</code> for every residue with more than one altloc, regardless of strategy. This flag alone controls altloc mapping; it is independent of <code>provenance_rules.record_original_mappings</code> (which governs the other four mapping types).</p>"},{"location":"policies/#assembly_rules","title":"<code>assembly_rules</code>","text":"<p><code>strategy</code> (default <code>preserve_as_reported</code>)</p> Value Description <code>preserve_as_reported</code> Keep assemblies as reported (aside from ID renumbering under <code>identifier_rules.assembly_id</code>). <code>standardize_biological_assembly</code> Intended to prefer the assembly annotated as biological, per <code>preferred_assembly_source</code>. Not implemented yet \u2014 currently identical to <code>preserve_as_reported</code>. <code>select_first_assembly</code> Keep only the first listed assembly, discard the rest. <p><code>preferred_assembly_source</code> (default <code>author</code>) \u2014 currently unused: no strategy reads this field yet.</p> Value Description <code>author</code> Use the assembly flagged by the depositing author as preferred. <code>pdbe</code> Use the PDBe-annotated preferred biological assembly. <code>pdbx</code> Use the <code>pdbx_struct_assembly</code> preferred flag from the mmCIF file. <code>first</code> Fall back to the first listed assembly if no preferred flag exists. <p><code>record_original_assembly_mapping</code> (default <code>True</code>) \u2014 populate <code>AssemblyMapping</code> for every assembly.</p>"},{"location":"policies/#entity_rules","title":"<code>entity_rules</code>","text":"<p><code>strategy</code> (default <code>preserve</code>)</p> Value Description <code>preserve</code> Keep entities and their original IDs. <code>standardize</code> Renumber entity IDs sequentially (<code>1</code>, <code>2</code>, ...). <code>merge_equivalent_entities</code> Merge entities that share an identical canonical polymer sequence (<code>pdbx_seq_one_letter_code_can</code>) into one entity ID. Non-polymer entities (no canonical sequence) are never merged with each other, even if their composition matches \u2014 only sequence-based merging for polymers is implemented. <p><code>preserve_original_entity_ids</code> (default <code>True</code>) \u2014 under <code>strategy: standardize</code>, controls whether <code>EntityMapping</code> records the pre-renumber ID (<code>True</code>) or just the new ID again (<code>False</code>).</p>"},{"location":"policies/#ligand_rules","title":"<code>ligand_rules</code>","text":"<p><code>strategy</code> (default <code>preserve</code>)</p> Value Description <code>preserve</code> Include every non-polymer entity as-is, including waters \u2014 see warning below. <code>filter</code> Apply <code>keep_waters</code> / <code>keep_ions</code> / <code>keep_nonpolymer_ligands</code> to decide what to drop. <code>annotate_only</code> Record ligands in diagnostics but exclude them from the canonical structure. <p><code>keep_waters</code> / <code>keep_ions</code> / <code>keep_nonpolymer_ligands</code> (default <code>True</code>) \u2014 only consulted under <code>strategy: filter</code>.</p> <p>Ion detection is a keyword heuristic, not a chemical-component lookup: an entity counts as an ion only if its <code>pdbx_description</code> contains one of a fixed set of keywords (<code>ION</code>, <code>ZINC</code>, <code>CALCIUM</code>, <code>SODIUM</code>, <code>CHLORIDE</code>, ...). An ion with a description that doesn't match falls into <code>keep_nonpolymer_ligands</code> instead of <code>keep_ions</code>.</p> <p><code>preserve</code> keeps waters too</p> <p><code>strategy: preserve</code> returns every atom unchanged, including waters \u2014 <code>keep_waters</code>/<code>keep_ions</code>/<code>keep_nonpolymer_ligands</code> are only consulted under <code>strategy: filter</code>. If you want waters excluded, use <code>strategy: filter</code> with <code>keep_waters: false</code>, not <code>preserve</code>.</p>"},{"location":"policies/#validation_rules","title":"<code>validation_rules</code>","text":"<p><code>strictness</code> (default <code>moderate</code>)</p> Value Description <code>strict</code> Promote every warning diagnostic to an error. <code>moderate</code> Default behavior \u2014 errors stay errors, warnings stay warnings. <code>permissive</code> Intended to downgrade error-level rules to warnings. Not implemented yet \u2014 accepted but has no effect. <p><code>fail_on_unresolved_issues</code> (default <code>True</code>) \u2014 if <code>True</code>, any unresolved error causes <code>status == \"failed\"</code>. <code>warnings_as_errors</code> (default <code>False</code>) \u2014 if <code>True</code>, any warning also causes <code>status == \"failed\"</code>.</p>"},{"location":"policies/#provenance_rules","title":"<code>provenance_rules</code>","text":"Field Description Default <code>record_original_mappings</code> Populate the chain/residue/assembly/entity mapping objects; when <code>False</code>, those four come back empty (smaller output). Does not affect <code>AltlocSelectionMapping</code> \u2014 that's gated only by <code>altloc_rules.record_selection</code>, independently of this flag. <code>True</code> <code>record_transforms</code> Intended to gate whether applied transforms are logged. Currently unused \u2014 transforms are always recorded. <code>True</code> <code>record_policy_application</code> Intended to embed the full applied policy in the result. Currently unused \u2014 the policy is never embedded. <code>True</code> <code>emit_canonicalisation_report</code> When <code>True</code>, include a warning/error count summary in <code>canonicalisationProvenance.report</code>. <code>False</code> <p>Naming drift from the design spec</p> <p>The design spec in <code>policies/canonicalisation.yaml</code> names the report flag <code>emit_canonicalization_report</code> (American spelling); the implemented schema field is <code>emit_canonicalisation_report</code> (British, matching the rest of the codebase). Use the schema's spelling.</p>"},{"location":"policies/#example-three-policies-one-structure","title":"Example: three policies, one structure","text":"<pre><code>from pandora.schemas.canonicalisation import (\n    canonicalisationPolicy,\n    IdentifierRules,\n    ChainIdRules,\n    ResidueNumberingRules,\n    AltlocRules,\n    AssemblyRules,\n    EntityRules,\n    LigandRules,\n)\n\n# Preserve everything as reported (including waters \u2014 see ligand_rules above).\npreserve_policy = canonicalisationPolicy(\n    policy_id=\"preserve\",\n    policy_name=\"Preserve As Reported\",\n    policy_version=\"1.0.0\",\n    identifier_rules=IdentifierRules(\n        chain_id=ChainIdRules(strategy=\"preserve\"),\n        residue_numbering=ResidueNumberingRules(strategy=\"preserve\"),\n    ),\n    altloc_rules=AltlocRules(strategy=\"select_best_occupancy\"),\n    ligand_rules=LigandRules(strategy=\"preserve\"),\n)\n\n# Remap chain IDs and renumber residues sequentially.\nremap_policy = canonicalisationPolicy(\n    policy_id=\"remap\",\n    policy_name=\"Remap Chains And Renumber\",\n    policy_version=\"1.0.0\",\n    identifier_rules=IdentifierRules(\n        chain_id=ChainIdRules(strategy=\"remap\"),\n        residue_numbering=ResidueNumberingRules(strategy=\"renumber\"),\n    ),\n    altloc_rules=AltlocRules(\n        strategy=\"select_best_occupancy\", tie_breaker=\"lowest_b_factor\"\n    ),\n    entity_rules=EntityRules(strategy=\"merge_equivalent_entities\"),\n    ligand_rules=LigandRules(\n        strategy=\"filter\", keep_waters=False, keep_ions=False\n    ),\n)\n\n# Keep author-assigned identifiers, strict validation.\nauthor_policy = canonicalisationPolicy(\n    policy_id=\"author\",\n    policy_name=\"Author Identifiers\",\n    policy_version=\"1.0.0\",\n    identifier_rules=IdentifierRules(\n        chain_id=ChainIdRules(strategy=\"use_auth_chain_id\"),\n        residue_numbering=ResidueNumberingRules(strategy=\"use_auth_seq\"),\n    ),\n    altloc_rules=AltlocRules(strategy=\"select_first\"),\n    assembly_rules=AssemblyRules(\n        strategy=\"standardize_biological_assembly\",\n        preferred_assembly_source=\"author\",\n    ),\n    ligand_rules=LigandRules(strategy=\"annotate_only\"),\n)\n</code></pre> <p>(This is the same set of policies used in <code>examples/overview.py</code>, minus the no-op <code>keep_waters=False</code> under the preserve policy \u2014 see the warning above.)</p>"},{"location":"policies/#provenance","title":"Provenance","text":"<p>Every rule that deviates from its \"preserve\" default is recorded as a transform label (e.g. <code>\"chain_id:remap\"</code>) in the returned <code>canonicalisationProvenance.transforms</code>, alongside the policy id/name/ version and, if <code>provenance_rules.emit_canonicalisation_report</code> is set, a report of warning/error counts. The original-to-canonical identifier mappings themselves come back separately as <code>CanonicalMappings</code>.</p>"},{"location":"recipes/","title":"Recipes","text":"<p>TODO</p>"},{"location":"reference/","title":"Reference","text":"<p>Auto-generated from docstrings. Only the public API of each package (what's exported via its <code>__init__.py</code>) is shown.</p>"},{"location":"reference/#pandora.ingestion","title":"<code>pandora.ingestion</code>","text":""},{"location":"reference/#pandora.ingestion.fetch_list_mmcif","title":"<code>fetch_list_mmcif(entry_ids, provider, source_uri, output_dir, fetch_options=None)</code>","text":"<p>Fetch a list of raw mmCIF files from a provider URL, write them to output_dir, return provenance for each.</p> <p>Calls <code>fetch_mmcif()</code> once per entry ID. If <code>fetch_options.allow_partial</code> is True, entries that fail to fetch (<code>RuntimeError</code> or <code>ValueError</code>) are skipped instead of aborting the whole batch.</p> <p>Parameters:</p> Name Type Description Default <code>entry_ids</code> <code>list[str]</code> <p>The PDB/PDBe entry identifiers to fetch.</p> required <code>provider</code> <code>str</code> <p>One of \"pdbe\" or \"pdb\", used to build the download URL when <code>source_uri</code> is not given.</p> required <code>source_uri</code> <code>str | None</code> <p>An explicit URL to fetch from, overriding <code>provider</code>'s default URL template, applied to every entry.</p> required <code>output_dir</code> <code>Path</code> <p>Directory to write the downloaded (or cached) files into; created if missing.</p> required <code>fetch_options</code> <code>FetchOptions | None</code> <p>Caching/decompression/retry options, including <code>allow_partial</code>. Defaults to <code>FetchOptions()</code> when not given.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[IngestionProvenance]</code> <p>A list of <code>IngestionProvenance</code>, one per successfully fetched</p> <code>list[IngestionProvenance]</code> <p>entry, in the same order as <code>entry_ids</code> (with failed entries</p> <code>list[IngestionProvenance]</code> <p>omitted when <code>allow_partial</code> is True).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>provider</code> is invalid and no <code>source_uri</code> was given, and <code>allow_partial</code> is False.</p> <code>RuntimeError</code> <p>If a fetch fails and <code>allow_partial</code> is False.</p>"},{"location":"reference/#pandora.ingestion.fetch_mmcif","title":"<code>fetch_mmcif(entry_id, provider, source_uri, output_dir, fetch_options=None)</code>","text":"<p>Fetch a raw mmCIF file from a provider URL, write it to output_dir, return provenance.</p> <p>Resolves the download URL from <code>source_uri</code> or from <code>provider</code> (\"pdbe\"/\"pdb\"), reuses a cached file when one is fresh (per <code>fetch_options</code>), otherwise downloads it, optionally decompresses it, and writes it to <code>output_dir</code>.</p> <p>Parameters:</p> Name Type Description Default <code>entry_id</code> <code>str</code> <p>The PDB/PDBe entry identifier (e.g. \"1abc\").</p> required <code>provider</code> <code>str</code> <p>One of \"pdbe\" or \"pdb\", used to build the download URL when <code>source_uri</code> is not given.</p> required <code>source_uri</code> <code>str | None</code> <p>An explicit URL to fetch from, overriding <code>provider</code>'s default URL template.</p> required <code>output_dir</code> <code>Path</code> <p>Directory to write the downloaded (or cached) file into; created if missing.</p> required <code>fetch_options</code> <code>FetchOptions | None</code> <p>Caching/decompression/retry options. Defaults to <code>FetchOptions()</code> when not given.</p> <code>None</code> <p>Returns:</p> Type Description <code>IngestionProvenance</code> <p><code>IngestionProvenance</code> describing the provider, source URL,</p> <code>IngestionProvenance</code> <p>retrieval timestamp, and whether the result came from cache.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>provider</code> is not \"pdbe\"/\"pdb\" and no <code>source_uri</code> was given.</p> <code>RuntimeError</code> <p>If the HTTP request fails, or the response cannot be decompressed/decoded.</p>"},{"location":"reference/#pandora.parsing","title":"<code>pandora.parsing</code>","text":""},{"location":"reference/#pandora.parsing.mmcif_to_structure","title":"<code>mmcif_to_structure(path_to_mmcif, model_num=1)</code>","text":"<p>Convert raw mmCIF text to a Structure (mmCIF data model).</p> <p>Parses the mmCIF file with gemmi, selects the requested model (falling back to the first model with a warning if not found), and extracts entities, atoms, asym units, connections, assemblies, and secondary structure into Pandora's typed <code>Structure</code> schema. Any other mmCIF categories are preserved verbatim in <code>Structure.raw</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path_to_mmcif</code> <code>str</code> <p>Path to the mmCIF file on disk.</p> required <code>model_num</code> <code>int</code> <p>Which model number to select from a multi-model entry.</p> <code>1</code> <p>Returns:</p> Type Description <code>tuple[Structure | None, DiagnosticBundle, ResultStatus]</code> <p>A tuple of: - The parsed <code>Structure</code>, or <code>None</code> if parsing failed. - A <code>DiagnosticBundle</code> of warnings/errors encountered. - A <code>ResultStatus</code>: \"success\", \"warning\", or \"failed\".</p>"},{"location":"reference/#pandora.canonicalisation","title":"<code>pandora.canonicalisation</code>","text":""},{"location":"reference/#pandora.canonicalisation.canonicalise_structure","title":"<code>canonicalise_structure(structure, policy)</code>","text":"<p>Convert a parsed structure into a canonical structure.</p> <p>Applies chain ID normalization, residue renumbering, assembly and entity normalization, missing-data handling, altloc resolution, and ligand filtering, in that order, according to the given policy. Each step records its mapping (for reversibility) and, if the step's strategy deviates from \"preserve\", appends a transform label to the returned provenance.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed input structure to canonicalise.</p> required <code>policy</code> <code>canonicalisationPolicy</code> <p>The canonicalisation policy whose rules govern each normalization step.</p> required <p>Returns:</p> Type Description <code>tuple[Structure, CanonicalMappings, canonicalisationProvenance]</code> <p>A tuple of: - The canonicalised <code>Structure</code>. - <code>CanonicalMappings</code> recording original-to-canonical   identifier mappings for chains, residues, assemblies,   entities, and altloc selections. - <code>canonicalisationProvenance</code> describing which policy was   applied, which transforms ran, and diagnostic counts.</p>"},{"location":"reference/#pandora.metadata","title":"<code>pandora.metadata</code>","text":""},{"location":"reference/#pandora.metadata.annotate_ligand_contacts","title":"<code>annotate_ligand_contacts(structure, distance_cutoff=4.0, include_waters=False)</code>","text":"<p>Compute polymer residues near non-polymer atoms within a cutoff.</p> <p>Groups HETATM records into ligands (by asym/auth_seq/comp id) and, for each ligand, finds polymer residues with at least one atom within <code>distance_cutoff</code> angstroms of a ligand atom.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The structure to scan for ligand contacts.</p> required <code>distance_cutoff</code> <code>float</code> <p>Contact distance in angstroms.</p> <code>4.0</code> <code>include_waters</code> <code>bool</code> <p>If True, treat water residues as ligands too.</p> <code>False</code> <p>Returns:</p> Type Description <code>AnnotationLayer</code> <p>An <code>AnnotationLayer</code> of type \"ligand_contacts\" whose <code>data</code></p> <code>AnnotationLayer</code> <p>holds the cutoff/flags used and, per ligand, the contacting</p> <code>AnnotationLayer</code> <p>polymer residues with their nearest distance.</p>"},{"location":"reference/#pandora.metadata.annotate_pairwise_sequence_identity","title":"<code>annotate_pairwise_sequence_identity(left, right)</code>","text":"<p>Compute a simple ungapped sequence identity between two entries.</p> <p>Compares every polymer entity in <code>left</code> against every polymer entity in <code>right</code> with a position-wise (ungapped, no alignment) identity, then reports the best-scoring pair.</p> <p>Parameters:</p> Name Type Description Default <code>left</code> <code>Structure</code> <p>The first structure to compare.</p> required <code>right</code> <code>Structure</code> <p>The second structure to compare.</p> required <p>Returns:</p> Type Description <code>AnnotationLayer</code> <p>An <code>AnnotationLayer</code> of type \"pairwise_sequence_identity\" whose</p> <code>AnnotationLayer</code> <p><code>data</code> holds the best identity score, the best-matching entity</p> <code>AnnotationLayer</code> <p>pair, and every entity-pair comparison.</p>"},{"location":"reference/#pandora.metadata.annotate_structure_counts","title":"<code>annotate_structure_counts(structure)</code>","text":"<p>Compute simple per-entry counts from the canonical structure.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The canonical structure to summarize.</p> required <p>Returns:</p> Type Description <code>AnnotationLayer</code> <p>An <code>AnnotationLayer</code> of type \"structure_counts\" whose <code>data</code></p> <code>AnnotationLayer</code> <p>holds atom/residue/asym/entity/assembly/connection counts,</p> <code>AnnotationLayer</code> <p>secondary-structure record counts, entity-type and atom-group</p> <code>AnnotationLayer</code> <p>breakdowns, and the altloc atom count.</p>"},{"location":"reference/#pandora.metadata.collect_metadata","title":"<code>collect_metadata(structure)</code>","text":"<p>Collect the basic source-backed metadata supported by Pandora today.</p> <p>Runs every <code>pandora.metadata.mmcif.extract_*</code> function against the structure and assembles the results into one record.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to collect metadata from.</p> required <p>Returns:</p> Type Description <code>MetadataRecord</code> <p>A <code>MetadataRecord</code> combining entry, quality, taxonomy, entity,</p> <code>MetadataRecord</code> <p>ligand, and UniProt-mapping metadata, plus the sorted list of</p> <code>MetadataRecord</code> <p>raw mmCIF category names present in the structure.</p>"},{"location":"reference/#pandora.metadata.extract_entity_metadata","title":"<code>extract_entity_metadata(structure)</code>","text":"<p>Extract entity-level descriptions, sequences, and chain membership.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract entity metadata from.</p> required <p>Returns:</p> Type Description <code>list[EntityMetadataRecord]</code> <p>An <code>EntityMetadataRecord</code> per entity, combining <code>_entity</code></p> <code>list[EntityMetadataRecord]</code> <p>fields (EC number, mutation, fragment), polymer sequence data,</p> <code>list[EntityMetadataRecord]</code> <p>and the chain (asym) ids belonging to that entity.</p>"},{"location":"reference/#pandora.metadata.extract_entry_metadata","title":"<code>extract_entry_metadata(structure)</code>","text":"<p>Extract entry-level source-backed metadata from the parsed structure.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract metadata from.</p> required <p>Returns:</p> Type Description <code>EntryMetadataRecord</code> <p>An <code>EntryMetadataRecord</code> with entry id, title, keywords, and</p> <code>EntryMetadataRecord</code> <p>citation details (title, DOI, PubMed id).</p>"},{"location":"reference/#pandora.metadata.extract_ligand_metadata","title":"<code>extract_ligand_metadata(structure, include_waters=False)</code>","text":"<p>Extract non-polymer ligand metadata from mmCIF categories.</p> <p>Reads <code>_pdbx_entity_nonpoly</code> (joined with <code>_chem_comp</code> for name and formula) for one record per distinct (entity, ligand) pair. Falls back to deriving ligand records from HETATM records in <code>structure.atoms</code> when neither category is present.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract ligands from.</p> required <code>include_waters</code> <code>bool</code> <p>If True, include water molecules as ligands.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[LigandMetadataRecord]</code> <p>A <code>LigandMetadataRecord</code> per distinct ligand entity.</p>"},{"location":"reference/#pandora.metadata.extract_metadata","title":"<code>extract_metadata(structure, category, keys=None)</code>","text":"<p>Compatibility wrapper around <code>extract_metadata_category()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to read raw categories from.</p> required <code>category</code> <code>str</code> <p>The mmCIF category name (e.g. \"_entity\").</p> required <code>keys</code> <code>Iterable[str] | None</code> <p>If given, restrict each row to these column names; otherwise return every column present in the category.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[RawRow]</code> <p>A list of rows, each a mapping from column name to its</p> <code>list[RawRow]</code> <p>(already null-normalized) string value.</p>"},{"location":"reference/#pandora.metadata.extract_metadata_category","title":"<code>extract_metadata_category(structure, category, columns=None)</code>","text":"<p>Return raw mmCIF rows for one category.</p> <p>This is the escape hatch for metadata that Pandora has not promoted to a typed schema yet.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to read raw categories from.</p> required <code>category</code> <code>str</code> <p>The mmCIF category name (e.g. \"_entity\").</p> required <code>columns</code> <code>Iterable[str] | None</code> <p>If given, restrict each row to these column names (missing columns come back as <code>None</code>); otherwise return every column present in the category.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[RawRow]</code> <p>A list of rows, each a mapping from column name to its</p> <code>list[RawRow]</code> <p>(already null-normalized) string value.</p>"},{"location":"reference/#pandora.metadata.extract_quality","title":"<code>extract_quality(structure)</code>","text":"<p>Extract experimental quality metrics reported in mmCIF.</p> <p>Reads the <code>_exptl</code>, <code>_refine</code>, and <code>_reflns</code> categories for the experimental method, resolution, R-work/R-free, reflection counts, and mean B-factor.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract quality data from.</p> required <p>Returns:</p> Type Description <code>QualityRecord | None</code> <p>A <code>QualityRecord</code>, or <code>None</code> if none of <code>_exptl</code>, <code>_refine</code>,</p> <code>QualityRecord | None</code> <p>or <code>_reflns</code> are present in the structure.</p>"},{"location":"reference/#pandora.metadata.extract_taxonomies","title":"<code>extract_taxonomies(structure)</code>","text":"<p>Extract source organism and expression host metadata.</p> <p>Reads the <code>_entity_src_gen</code>, <code>_entity_src_nat</code>, and <code>_pdbx_entity_src_syn</code> mmCIF categories, covering genetically manipulated, naturally isolated, and synthetic sources respectively.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract taxonomies from.</p> required <p>Returns:</p> Type Description <code>list[TaxonomyRecord]</code> <p>A <code>TaxonomyRecord</code> per source row found, in category order</p> <code>list[TaxonomyRecord]</code> <p>(generated, then natural, then synthetic sources).</p>"},{"location":"reference/#pandora.metadata.extract_taxonomy","title":"<code>extract_taxonomy(structure)</code>","text":"<p>Return the first taxonomy record, if present.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract taxonomy from.</p> required <p>Returns:</p> Type Description <code>TaxonomyRecord | None</code> <p>The first <code>TaxonomyRecord</code> from <code>extract_taxonomies()</code>, or</p> <code>TaxonomyRecord | None</code> <p><code>None</code> if the structure has no source records.</p>"},{"location":"reference/#pandora.metadata.extract_uniprot_mappings","title":"<code>extract_uniprot_mappings(structure)</code>","text":"<p>Extract UniProt/SIFTS mappings reported in the mmCIF file.</p> <p>Reads <code>_struct_ref</code>/<code>_struct_ref_seq</code> and <code>_pdbx_sifts_unp_segments</code>, deduplicating mappings that describe the same entity/asym/accession/range.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The parsed structure to extract mappings from.</p> required <p>Returns:</p> Type Description <code>list[UniProtMappingRecord]</code> <p>A <code>UniProtMappingRecord</code> per distinct mapping segment found</p> <code>list[UniProtMappingRecord]</code> <p>across both source categories.</p>"},{"location":"reference/#pandora.annotations","title":"<code>pandora.annotations</code>","text":""},{"location":"reference/#pandora.annotations.annotate_ligand_contacts","title":"<code>annotate_ligand_contacts(structure, distance_cutoff=4.0, include_waters=False)</code>","text":"<p>Compute polymer residues near non-polymer atoms within a cutoff.</p> <p>Groups HETATM records into ligands (by asym/auth_seq/comp id) and, for each ligand, finds polymer residues with at least one atom within <code>distance_cutoff</code> angstroms of a ligand atom.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The structure to scan for ligand contacts.</p> required <code>distance_cutoff</code> <code>float</code> <p>Contact distance in angstroms.</p> <code>4.0</code> <code>include_waters</code> <code>bool</code> <p>If True, treat water residues as ligands too.</p> <code>False</code> <p>Returns:</p> Type Description <code>AnnotationLayer</code> <p>An <code>AnnotationLayer</code> of type \"ligand_contacts\" whose <code>data</code></p> <code>AnnotationLayer</code> <p>holds the cutoff/flags used and, per ligand, the contacting</p> <code>AnnotationLayer</code> <p>polymer residues with their nearest distance.</p>"},{"location":"reference/#pandora.annotations.annotate_pairwise_sequence_identity","title":"<code>annotate_pairwise_sequence_identity(left, right)</code>","text":"<p>Compute a simple ungapped sequence identity between two entries.</p> <p>Compares every polymer entity in <code>left</code> against every polymer entity in <code>right</code> with a position-wise (ungapped, no alignment) identity, then reports the best-scoring pair.</p> <p>Parameters:</p> Name Type Description Default <code>left</code> <code>Structure</code> <p>The first structure to compare.</p> required <code>right</code> <code>Structure</code> <p>The second structure to compare.</p> required <p>Returns:</p> Type Description <code>AnnotationLayer</code> <p>An <code>AnnotationLayer</code> of type \"pairwise_sequence_identity\" whose</p> <code>AnnotationLayer</code> <p><code>data</code> holds the best identity score, the best-matching entity</p> <code>AnnotationLayer</code> <p>pair, and every entity-pair comparison.</p>"},{"location":"reference/#pandora.annotations.annotate_structure_counts","title":"<code>annotate_structure_counts(structure)</code>","text":"<p>Compute simple per-entry counts from the canonical structure.</p> <p>Parameters:</p> Name Type Description Default <code>structure</code> <code>Structure</code> <p>The canonical structure to summarize.</p> required <p>Returns:</p> Type Description <code>AnnotationLayer</code> <p>An <code>AnnotationLayer</code> of type \"structure_counts\" whose <code>data</code></p> <code>AnnotationLayer</code> <p>holds atom/residue/asym/entity/assembly/connection counts,</p> <code>AnnotationLayer</code> <p>secondary-structure record counts, entity-type and atom-group</p> <code>AnnotationLayer</code> <p>breakdowns, and the altloc atom count.</p>"},{"location":"reference/#schemas","title":"Schemas","text":"<p>Typed Pydantic models used throughout the API above.</p>"},{"location":"reference/#pandora.schemas.canonicalisation","title":"<code>pandora.schemas.canonicalisation</code>","text":""},{"location":"reference/#pandora.schemas.structure","title":"<code>pandora.schemas.structure</code>","text":""},{"location":"reference/#pandora.schemas.structure.ConfRecord","title":"<code>ConfRecord</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>One row from _struct_conf (HELX_P, TURN_P, STRN, etc.).</p>"},{"location":"reference/#pandora.schemas.metadata","title":"<code>pandora.schemas.metadata</code>","text":""},{"location":"reference/#pandora.schemas.annotation","title":"<code>pandora.schemas.annotation</code>","text":""},{"location":"reference/#pandora.schemas.ingestion","title":"<code>pandora.schemas.ingestion</code>","text":""},{"location":"reference/#pandora.schemas.common","title":"<code>pandora.schemas.common</code>","text":""},{"location":"reference/#pandora.schemas.common.AppliedPolicyRef","title":"<code>AppliedPolicyRef</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Compact reference to a policy that was applied at a pipeline stage.</p>"}]}